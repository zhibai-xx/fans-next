# 2026-02-12 上线最小交付清单（前端）
- 决策：在未购买域名/云服务前，先完成前端可发布基线并冻结高风险改动
- 阶段一（本地完工标准）：
- `npm run lint` 全绿
- 登录、过期会话、登出、互动状态 4 条主流程手测通过
- 前端 API 统一走相对路径 `/api/*`，禁止直连后端域名
- 阶段二（上云前准备）：
- 明确线上 `NEXTAUTH_URL` 与 `NEXTAUTH_SECRET`
- 明确前端站点域名（与后端 CORS 域名一致）
- 阶段三（部署落地）：
- Nginx 对外暴露 80/443，仅转发前端 3001 与后端 API
- 首次上线验证：首页、登录页、个人中心、图片页、视频页
- 阶段四（可用性）：
- 使用免费 Uptime 探测首页与关键 API（`/api/health`）
- 记录一次完整回滚步骤（回退前端构建版本）
- 影响：降低前期投入，优先保证登录鉴权链路稳定上线
